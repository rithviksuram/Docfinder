// This file can be safely deleted as the functionality has been moved to the auth slice
// If you need to keep this file for other auth utilities, you can use this template:

import { store } from '../store';
import { API_BASE_URL } from '../config';

// Add any additional auth utilities here that aren't already in the auth slice 